# Dignite Vault Extract Agent Guide

The truth source for this project's positioning, architecture rules, OUT-of-scope list, Markdown-first contract, six-stage ETO event contract, security covenant, and decision principles is [CLAUDE.md](./CLAUDE.md). Read it first.

For ABP / DDD / EF Core / multi-tenancy / background-job / module-template / testing conventions referenced by CLAUDE.md, see [`.claude/rules/`](./.claude/rules/). Load only the files relevant to the current task.

This file exists for tools (Codex CLI, etc.) that look for `AGENTS.md` by convention. It is intentionally a pointer, not a duplicate — keeping the source of truth in one place prevents drift.
